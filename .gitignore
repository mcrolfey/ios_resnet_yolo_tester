# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint

# Swift Package Manager
.swiftpm/
.build/

# macOS
.DS_Store

# CoreML model weights — exported locally via MLModels/export_to_coreml.ipynb,
# not committed (same convention as the Android app's ONNX weights).
*.mlpackage/

# Jupyter
.ipynb_checkpoints/
